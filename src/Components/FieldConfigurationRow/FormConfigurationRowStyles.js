import { createStyles } from "@material-ui/core";

export default createStyles({
    row: {
        flex: 1,
        flexDirection: "row",
    },
    rowItem: {
        margin: 10,
        minWidth: 100,
    },
    button: {
        marginTop: 20,
        marginLeft: 50,
    }
})